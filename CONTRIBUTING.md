# Contributing

Run `moon fmt --check`, all target checks, and `moon test --target wasm-gc` before opening a change. Keep the wire format deterministic and add a negative test for every new diagnostic.
